How do I copy a visual selection to clipboard?

⏺ To copy a visual selection to the clipboard in Neovim:

  Basic approach:
  1. Make a visual selection (press v for character-wise, V for line-wise, or <C-v> for block-wise)
  2. Press "+y to yank to the system clipboard

  The "+ register is the system clipboard. You can also use "* on some systems.

  Alternative:
  If clipboard=unnamedplus is set in your config, then simply pressing y in visual mode will copy to the system clipboard automatically.

  Let me check your current clipboard configuration:

⏺ Searched for 1 pattern (ctrl+o to expand)

⏺ In your config, clipboard is handled differently:

  Your config uses a hybrid clipboard approach - vim registers and system clipboard are kept separate:

  - To copy visual selection to clipboard: Select text in visual mode, then press <leader>y (which is <space>y)
  - To paste from clipboard: <leader>p (which is <space>p)

  Quick reference (from lua/kyleking/core/keymaps.lua:20-23):
  - <space>y - Yank to clipboard (works in visual mode)
  - <space>Y - Yank line to clipboard
  - <space>p - Paste from clipboard
  - <space>P - Paste before from clipboard
  - <C-v> in insert mode - Paste from clipboard

  This approach keeps regular y/p operations using vim's internal registers for your normal workflow, while leader-prefixed keys interact with the system clipboard.

❯ <leader>y doesn't seem to be working for me

⏺ Let me help debug this. First, let me verify the keymaps are set up correctly and check your leader key:

⏺ Read 1 file (ctrl+o to expand)

⏺ The keymaps look correct. Let me help you troubleshoot. Can you try these diagnostic steps in Neovim:

  1. Check if the keymap exists:
  :verbose nmap <leader>y

  2. Test clipboard access directly:
  :let @+ = "test"
  :echo @+

  3. Check clipboard support:
  :echo has('clipboard')

  What specifically happens when you try <leader>y?
  - Nothing happens?
  - An error appears?
  - Text gets yanked but not to clipboard?

  Also, what are you doing exactly?
  1. Visual select some text with v
  2. Press <space> then y?

  Let me also check if there might be any plugin conflicts:

⏺ Searched for 1 pattern (ctrl+o to expand)

⏺ No conflicts found. The keymap is only defined in one place. Let me check if mini.clue (your which-key alternative) might be intercepting the key:

⏺ Searched for 2 patterns, read 3 files (ctrl+o to expand)

⏺ Found it! There's a 500ms delay configured for mini.clue (the which-key popup). This delay shouldn't prevent <leader>y from working, but it might make the interaction feel
  different.

  Here's what should happen:
  1. Press <space> (leader key)
  2. Press y within any reasonable time
  3. Text gets yanked to clipboard (no popup appears if you're fast)
  4. If you wait >500ms after <space>, the clue popup appears showing available keys

  Try this diagnostic sequence:

  1. Test if the keymap exists at all:
  :nmap <leader>y
  1. Should show: "*y  Yank to clipboard
  2. Test clipboard access directly:
  :let @+ = "direct test"
  2. Then paste in another app - does "direct test" appear?
  3. Check Neovim clipboard support:
  :checkhealth provider
  3. Look for clipboard provider status
  4. Test in visual mode specifically:
    - Select some text with v
    - Type <space>y (quickly, don't wait for popup)
    - Try pasting in another app

  If it still doesn't work, please tell me:
  - What happens when you press <space>y? (nothing, error, yank but not to clipboard?)
  - Does :checkhealth provider show clipboard as working?
  - Are you on macOS/Linux/Windows?

✻ Sautéed for 49s

❯ With my config, how do I move a line from one location to another? (e.g. move down two lines without indentation changes)

  And in more complicated scenarios: I have a common issue that Python imports end up within functions and need to delete the line they are on, the line below, and then
  paste them at the top of the file while removing the indent. There may be one or more of these imports. My existing workflow was to cut to clipboard, go to the top of the
   file, paste, and unindent. With the clipboard changes, is there a better way to simplify this operation?

⏺ Let me check what movement tools are available in your config:

⏺ Searched for 2 patterns, read 2 files (ctrl+o to expand)

⏺ Perfect! Your config has mini.move for this. Here's how to handle both scenarios:

  Simple Line Movement

  Move line down 2 positions without indent change:
  - Position cursor on the line
  - Press <leader>mj twice (that's <space>mj twice)

  The documentation says it "Auto-indents when moving vertically" - this maintains proper code structure. If you truly need NO indent adjustment, you'd use the classic vim
  approach: ddp (delete line, paste below).

  Python Import Workflow

  For your import cleanup scenario, here are the best approaches with your hybrid clipboard setup:

  Option 1: Using Named Registers (Most Flexible)

  1. Position cursor on import line
  2. Visual select lines: Vjj (or however many lines)
  3. Delete to register 'a': "ad
  4. Go to top: gg
  5. Paste: "ap
  6. Unindent repeatedly: << (or visual select and <)

  Option 2: Quick Default Register Workflow

  1. Visual select import lines: Vjj
  2. Delete: d (goes to default register)
  3. Go to top: gg
  4. Paste above: P
  5. Fix indent: == (auto-indent line) or << (dedent once)

  Option 3: Multiple Imports with Markers

  1. Mark top of file: gg, then ma (set mark 'a')
  2. Navigate to import: /import or search
  3. Delete lines: Vjjd
  4. Jump to mark: 'a
  5. Paste: p
  6. Fix indent: <<
  7. Repeat for next import (/ to continue search)

  Let me check if there are any indent-related keymaps configured:

⏺ Searched for 1 pattern (ctrl+o to expand)

⏺ Standard vim indent keys are available:
  - << - Dedent current line
  - >> - Indent current line
  - In visual mode: < dedent, > indent
  - == - Auto-indent current line

  Streamlined Workflow Recommendation

  For your Python imports use case, I'd recommend:

  Single import:
  Vjjd    " Visual select + delete (2-3 lines)
  ggP     " Go to top, paste above first line
  <<      " Dedent (repeat as needed, or use visual mode)

  Multiple imports (repeat pattern):
  ma      " Mark current position as 'a'
  /import " Search for import
  Vjjd    " Delete import block
  ggP     " Paste at top
  <<      " Fix indent
  'a      " Return to mark
  n       " Next search result

  Key advantages of this approach:
  - Uses default register (no prefix needed)
  - P (paste above) keeps imports in order at top
  - Marks let you bounce back and forth
  - n continues your search pattern

  Would you like me to create a custom command or keymap to automate this "cut, go to top, paste, dedent" workflow?
