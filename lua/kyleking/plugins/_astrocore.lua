return {
  {
    "AstroNvim/astrocore",
    enabled = false, -- PLANNED: Look into URL Highlighting, which might be using a separate plugin
    opts = {
      features = {
        highlighturl = true, -- highlight URLs by default
      },
    },
  },
}
